{{ attach_library('module_hero/hero.style') }}
<h4 class='animate__animated animate__bounce'>{{title}}</h4>
<ol>
  {% for item in items %}
    {# <li class="animate__animated animate__{{ random(['flash','pulse','swing', 'heartBeat']) }}">{{item.name}}</li> #}
    <li>{{item.name}}</li>
  {% endfor %}
</ol>
